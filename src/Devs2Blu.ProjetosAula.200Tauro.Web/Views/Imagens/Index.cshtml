@model IEnumerable<Devs2Blu.ProjetosAula._200Tauro.Web.Models.Entities.Imagem>
<link rel="stylesheet" href="~/css/titulos.css" asp-append-version="true" />
<h1 class="title">Imagem</h1>

<p class="title">
    <button class="creat"><a asp-action="Create">Create New</a> </button>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EnderecoImagem)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Conteudo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EnderecoImagem)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Conteudo.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
